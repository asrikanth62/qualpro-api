server.port=8080
# Optional: keep root context (clean URL). If you want /qualpro-api prefix, uncomment:
# server.servlet.context-path=/qualpro-api
management.endpoints.web.exposure.include=health,info
